---
name: use-design-system
description: Enforce project design system for all frontend work
---

Always consult frontend/design-system before generating UI.

Rules:
- Never invent arbitrary colors, spacing, radius, or typography
- Reuse existing design tokens/components first
- Extend the system only when necessary
- Preserve visual consistency across all new interfaces
- New UI must look native to the existing product